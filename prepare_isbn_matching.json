[
  {
    "op": "core/column-addition",
    "engineConfig": {
      "facets": [],
      "mode": "row-based"
    },
    "baseColumnName": "ISBN",
    "expression": "grel:filter(value.split(\";\"),v,v.trim().length()==13).sort()[0].trim() # extract the sorted 13-digit ISBN and remove any spaces",
    "onError": "set-to-blank",
    "newColumnName": "isbn13",
    "columnInsertIndex": 5,
    "description": "Create column isbn13 at index 5 based on column ISBN using expression grel:filter(value.split(\";\"),v,v.trim().length()==13).sort()[0].trim() # extract the sorted 13-digit ISBN and remove any spaces"
  },
  {
    "op": "core/column-addition",
    "engineConfig": {
      "facets": [],
      "mode": "row-based"
    },
    "baseColumnName": "ISBN",
    "expression": "grel:filter(value.split(\";\"),v,v.trim().length()==10).sort()[0].trim() # extract the sorted 10-digit ISBN and remove any spaces",
    "onError": "set-to-blank",
    "newColumnName": "isbn10",
    "columnInsertIndex": 5,
    "description": "Create column isbn10 at index 5 based on column ISBN using expression grel:filter(value.split(\";\"),v,v.trim().length()==10).sort()[0].trim() # extract the sorted 10-digit ISBN and remove any spaces"
  },
  {
    "op": "core/column-addition",
    "engineConfig": {
      "facets": [],
      "mode": "row-based"
    },
    "baseColumnName": "ISBN",
    "expression": "grel:coalesce(cells[\"isbn13\"].value,cells[\"isbn10\"].value)",
    "onError": "set-to-blank",
    "newColumnName": "isbn_matching",
    "columnInsertIndex": 5,
    "description": "Create column isbn_matching at index 5 based on column ISBN using expression grel:coalesce(cells[\"isbn13\"].value,cells[\"isbn10\"].value)"
  }
]
